# wr-ai

一个用于管理 Claude AI 配置（Commands 和 Skills）的命令行工具。

## ✨ 特性

- 🚀 **快速配置**: 从 GitHub 仓库快速获取和安装 Claude 配置
- 📦 **分组管理**: 按 Commands 和 Skills 分类管理
- 🔄 **智能合并**: 合并模式保留本地独有文件，避免数据丢失
- 🎨 **美观界面**: 彩色交互式命令行界面
- ⚡ **批量操作**: 支持批量添加、更新配置
- 🔒 **自动保护**: 自动更新 `.gitignore`，保护配置隐私

## 📦 安装

```bash
# 使用 npm
npm install -g wr-ai

# 使用 pnpm
pnpm add -g wr-ai

# 使用 yarn
yarn global add wr-ai
```

## 🚀 快速开始

### 1. 设置 GitHub 仓库地址

```bash
wr-ai set github <仓库地址>
```

例如：

```bash
wr-ai set github https://github.com/woicw/ai-config.git
# 或简写形式
wr-ai set github woicw/ai-config
```

### 2. 查看可用配置

```bash
wr-ai list
```

### 3. 初始化配置

```bash
wr-ai init
```

交互式选择要添加的 Commands 和 Skills。

### 4. 快速添加单个配置

```bash
# 添加 command
wr-ai add commit

# 添加 skill
wr-ai add mcp-builder
```

### 5. 更新配置

```bash
wr-ai update
```

## 📖 命令详解

### `set github <url>`

设置 GitHub 远程仓库地址，用于获取配置源。

```bash
wr-ai set github https://github.com/user/repo.git
```

**说明**:

- 配置保存在 `~/.wr-ai/config.json`
- 默认仓库: `https://github.com/woicw/ai-config.git`
- 支持完整 URL 或简写形式（如 `user/repo`）

### `init`

交互式初始化配置到当前目录的 `.claude/` 文件夹。

**功能**:

- 默认从 `awesome-claude/` 目录读取配置
- 支持按 Commands 和 Skills 分组选择
- 支持批量选择（ALL、ALL Commands、ALL Skills）
- 合并模式：保留本地独有文件，覆盖已存在的文件
- 自动更新 `.gitignore`

**选择界面**:

```
? 请选择要添加的配置（空格选择，回车确认）:
  ◻ ⚡ ALL - 复制所有配置
  ◻ 🔧 ALL Commands          (全部 2 个)
     ◻ ○ commit
     ◻ ○ review
  ◻ 🧠 ALL Skills            (全部 4 个)
     ◻ ○ code-review
     ◻ ○ mcp-builder
     ...
```

**合并行为**:

- 远程存在、本地不存在 → **新增**
- 远程存在、本地也存在 → **更新**（覆盖）
- 本地存在、远程不存在 → **保留**本地文件

### `list`

列出所有可用的 Commands 和 Skills。

**输出示例**:

```
┌ 📦 awesome-claude ──────────────────────────┐
│                                             │
│  🔧 Commands (2)                            │
│                                             │
│  ├─ commit                                  │
│  └─ review                                  │
│                                             │
│  🧠 Skills (4)                              │
│                                             │
│  ├─ code-review                             │
│  ├─ mcp-builder                             │
│  ├─ react-webapp-builder                    │
│  └─ skill-creator                            │
│                                             │
└─────────────────────────────────────────────┘
```

### `add <name>`

快速添加指定的 command 或 skill。

```bash
# 添加 command
wr-ai add commit

# 添加 skill
wr-ai add mcp-builder
```

**行为**:

- 自动识别是 command（`.md` 文件）还是 skill（目录）
- 复制到对应的 `.claude/commands/` 或 `.claude/skills/` 目录
- 如果未找到，会列出所有可用的选项

### `update`

更新已安装的配置。

**功能**:

- 与 `init` 相同的交互式选择界面
- 合并模式更新，保留本地独有文件
- 显示新增和更新的文件数量

### `clear`

清除本地缓存（包括配置和模板）。

```bash
wr-ai clear
```

**说明**:

- 删除 `~/.wr-ai/` 目录
- 不影响项目中的 `.claude/` 目录

## 📁 目录结构

### 远程仓库结构

```
ai-config/
├── .claude/
│   ├── commands/
│   │   ├── commit.md
│   │   └── review.md
│   └── skills/
│       └── code-review/
│           ├── README.md
│           └── SKILL.md
├── awesome-claude/          # 默认配置源
│   ├── commands/
│   │   ├── commit.md
│   │   └── review.md
│   └── skills/
│       ├── code-review/
│       ├── mcp-builder/
│       ├── react-webapp-builder/
│       └── skill-creator/
├── .gitignore
└── package.json
```

### 本地输出结构

所有配置都会复制到项目根目录的 `.claude/` 文件夹：

```
项目根目录/
└── .claude/
    ├── commands/
    │   ├── commit.md          # Git 提交命令
    │   ├── review.md          # 代码审查命令
    │   └── webapp.md         # Web 应用命令
    └── skills/
        └── code-review/       # 代码审查技能
            ├── README.md
            └── SKILL.md
      
```

## 🔧 配置

配置文件位置: `~/.wr-ai/config.json`

```json
{
  "origin": "https://github.com/woicw/ai-config.git"
}
```

## 💡 使用场景

1. **初始化新项目**: 使用 `wr-ai init` 快速添加常用的 Commands 和 Skills
2. **添加单个功能**: 使用 `wr-ai add <name>` 快速添加特定配置
3. **更新配置**: 使用 `wr-ai update` 同步远程最新配置
4. **查看可用配置**: 使用 `wr-ai list` 浏览所有可用选项

## 🛠️ 技术栈

- **Node.js**: ES Modules
- **Commander**: 命令行参数解析
- **@clack/prompts**: 交互式命令行界面
- **ora**: 加载动画
- **yoctocolors**: 终端颜色输出

## 📝 开发

```bash
# 克隆仓库
git clone https://github.com/woicw/wr-ai.git
cd wr-ai

# 安装依赖
pnpm install

# 本地开发（需要全局链接）
pnpm link --global
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

---

**提示**: 使用 `wr-ai --help` 查看所有可用命令。
